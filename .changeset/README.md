# Changesets

Run `pnpm changeset` for changes that affect the public `@keenpix/*` SDK or framework packages. The public package family is versioned together and each package overrides the default access with `publishConfig.access: public`.
