---
title: Analytics
description: Every request is logged — here's what you get for free.
icon: ChartArea
---

Analytics is built in, not bolted on. Every request to `/img/*` writes a log row
(timestamp, project, source host, path, width, format, status, cache hit/miss,
latency, bytes in/out) and updates an hourly Postgres rollup. Live Logs read the
raw rows; dashboard analytics read the rollups.

## What you get

- **KPIs** — total requests, bandwidth saved, cache hit-rate, and p95 latency, each with
  a real trend versus the previous window.
- **Requests over time** — cache hits vs live-optimized, with a range toggle (24h–90d).
- **Format distribution** — how much traffic each output format receives.
- **Response latency** — a p50/p95/p99 breakdown and a histogram.
- **Top images** — the most-requested paths.
- **Live logs** — every request, newest first, with faceted filters (format, status,
  cache, and source domain when a single project is selected).
- **Optional Cloudflare edge analytics** — edge hit-rate, edge requests, bytes served
  from Cloudflare, and hourly edge hit/miss split for `/img/*`.

## Scope: all projects or one

The switcher at the top of the sidebar sets the scope. **All projects** shows org-wide
totals plus a per-project breakdown; pick a single project to scope every chart, the
logs, and the KPIs to it.

<Callout type="info">
  Origin-shield analytics are computed from Postgres rollups, with raw logs kept for
  live/debug views. Cloudflare edge analytics are pulled separately from Cloudflare's
  GraphQL Analytics API because edge hits never reach the Keenpix server.
</Callout>
