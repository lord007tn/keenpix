# Keenpix — Implementation Status

> Living document. Updated as work lands. See [`PLAN.md`](./PLAN.md) for the full plan.

**Last updated:** 2026-06-13

---

## TL;DR — where we are

**Phases 0–8 done — Keenpix is a working product end-to-end.** The image optimizer uses the CDN-friendly path endpoint `GET /img/<source-url>?project=&w=&fmt=…`, fetches an allowlisted origin image, transforms it with **sharp**, caches variants on disk and in memory, and logs every request to Postgres — guarded by an origin allowlist + private-IP/SSRF block. **better-auth email OTP** protects `/app`, with SMTP managed from Settings and env fallback. **Prisma + Postgres** with seed data powers projects, logs, settings, staff, internal API keys, and SDK activity. The UI (marketing/self-host splash, overview, analytics, live logs, operations, settings, account, docs) runs on real data through the 4-layer architecture.

**Recent analytics state:** source-domain capture, per-project/domain breakdowns, SSE live logs, operations cache/queue health, optional Cloudflare GraphQL edge-cache analytics, and hourly Postgres analytics rollups are live. Overview and Analytics now separate Cloudflare edge delivery from Keenpix origin-shield metrics, with a fixed 24h edge window matching Cloudflare's adaptive dataset limits.

**Remaining:** daily/weekly rollup tables plus raw-log retention, geographic analytics, per-URL/per-project Cloudflare edge attribution via Logpush or a richer edge ingest, and an optional ClickHouse sink only for unusually high-volume installs.

---

## Phase checklist

- [x] **Phase 0 — Scaffold & tooling**
  - [x] TanStack Start app initialized (shadcn `start` template, base-ui, Tailwind v4) at repo root
  - [x] `@/*` path alias, tsconfig, vite config (from scaffold)
  - [x] Biome + Ultracite
  - [x] Layer folders created (routes, functions, actions, data-access, db, lib, components, features, shared, stores)
  - [x] shadcn initialized (`components.json`, Tailwind v4, OKLCH, vega base)
  - [x] Theme: brand orange primary + teal chart palette + **success / warning / info** tokens
  - [x] `git init`
  - [x] Dev server boots (HTTP 200) + `src/README.md` structure doc + `shared/format.ts`
  - [x] Nitro Node production build
- [x] **Phase 1 — App shell + Dashboard** — top-tab app shell, project switcher, Overview dashboard with source-split analytics, recent activity, project table, and operations summary
- [x] **Phase 2 — Analytics / Logs / Docs / Settings** — analytics source-split cards/charts, filters, project/domain breakdowns, SSE live logs, Fumadocs docs, project/global settings, operations, staff, SMTP, and internal API keys
- [x] **Phase 3 — Marketing landing + auth/OTP UI** — landing at `/` (hero, features, self-host, CTA); login = email→OTP flow (shadcn `InputOTP`)
- [x] **Phase 4 — Prisma + Postgres + seeders + wire data-access** — schema (Org/Project/Token/RequestLog + better-auth models), `prisma.config.ts` (Prisma 7 + PrismaPg), migration applied, seed (3 projects/2 tokens/800 logs), `data-access` on Prisma
- [x] **Phase 5 — better-auth email OTP + route protection** — `emailOTP` plugin, SMTP/env delivery with dev console fallback, `tanstackStartCookies`, `/api/auth/$` handler, `/app` gated; full sign-in verified end-to-end
- [x] **Phase 6 — `/img/*` sharp endpoint** — fetch → sharp resize/re-encode (avif/webp/jpeg/png, Accept-negotiated or explicit `fmt`) → disk cache (HIT/MISS) → `Cache-Control: immutable`; origin allowlist + SSRF guard; request logging. The source URL now lives in the request path so Cloudflare and other CDNs can see the source file extension.
- [x] **Phase 7 — Real analytics from request_logs + Postgres rollups** — summary, timeseries, format distribution, status/domain filters, source-domain capture, top images, approximate latency percentiles/histogram, project/domain breakdowns, dashboard deltas, SSE live logs, and optional Cloudflare edge rollups
- [x] **Phase 8 — Docker** — `Dockerfile` (node:22-slim, multi-stage, `prisma migrate deploy` on boot) + `docker-compose.yml` (app + `postgres:18-alpine` postgres:passer + volumes) + `.dockerignore`; `docker compose config` valid
- [ ] **Phase 9 (later) — ClickHouse for analytics** — deferred per user
- [x] **Task #9 — husky → Biome + Ultracite**

---

## What works right now (verified)
- `pnpm dev` → app at `http://localhost:3000`. Marketing `/` (200), `/login` (200), `/app/*` auth-gated (307 → /login without session).
- **Image transform:** `GET /img/https://cdn.example.com/hero.jpg?project=store&w=400&fmt=webp` → optimized image with immutable cache headers; `403` on private-IP (SSRF) and on origins outside the project allowlist. `/api/keenpix` is intentionally gone.
- **Auth:** `/api/auth/ok` 200; email→OTP sign-in creates a user + session and unlocks `/app` (verified end-to-end). OTP delivery uses Settings SMTP, env SMTP fallback, or console fallback in dev.
- **DB:** Postgres `keenpix` — 3 projects, 800+ request logs (incl. real ones from transforms), 2 users. `pnpm db:seed` / `db:migrate` / `db:studio` wired.
- **UI on real data:** overview, analytics, live logs, operations, settings, docs, account, staff, SMTP, and internal API keys.
- **Postgres analytics rollups:** new request logs update hourly rollup rows transactionally; migration backfills existing logs. Overview/Analytics read rollups, while Live Logs still reads raw rows.
- **Optional Cloudflare edge analytics:** Settings → CDN cache stores encrypted token/zone/host settings; analytics/dashboard show edge hit-rate, edge requests, edge bytes, and hourly edge hit/miss when configured.
- Theme: orange `--primary`, branded `--chart-*`, `--success`/`--warning`/`--info` (+`-foreground`) light+dark; `cva` variants on Button & Badge.
- `pnpm typecheck` clean. Docker: `docker compose up --build` (app + Postgres).

## What's mocked / fixtures (intentional)
- Seed data is still used for local demos and fresh development databases.
- OTP can fall back to console delivery in dev when SMTP is not configured.
- Geographic analytics are not implemented yet; request logs do not currently populate country/region.

## Decisions log
| Date | Decision | Rationale |
|---|---|---|
| 2026-05-22 | Self-host now, SaaS-ready later | User direction; keep `orgId` scoping from day one |
| 2026-05-22 | Node + sharp (TS) | User direction; sharp needs Node, not Cloudflare Workers |
| 2026-05-22 | App runs as Node container; Cloudflare = CDN in front | sharp can't run on Workers; CDN caches the transform endpoint |
| 2026-05-31 | Transform endpoint moved to `/img/<source-url>` | Keeps the source file extension visible to CDNs and avoids an API-looking cache path |
| 2026-05-22 | DB = Prisma + Postgres | User override; self-host deployment uses Postgres from day one |
| 2026-05-22 | Mirror fluent-seo's 4-layer arch (data-access/actions/functions/routes) | User asked to follow reference structure |
| 2026-05-22 | better-auth email OTP, configured fresh | Not in references; user requirement |
| 2026-05-22 | shadcn tokens only + add success/warning/info | User requirement; no Tailwind default colors |

## Known gaps / open questions
- Whether Paraglide i18n is needed for v1 (deferred for now).
- How aggressive raw-log retention should be once daily/weekly rollups exist.

## Changelog
- **2026-05-22** — Mapped safhati + fluent-seo, read skills (shadcn, composition, react-best-practices, frontend-design), reviewed TanStack Intent. Wrote `PLAN.md` + this file.
- **2026-05-22** — Phase 0: scaffolded TanStack Start + shadcn (relocated to repo root), `git init`, applied Keenpix theme + success/warning/info tokens, created layer folders + `src/README.md` + `shared/format.ts`. Verified dev server boots (HTTP 200).
- **2026-05-22** — Stack updated per user: **Docker deploy (not CF Workers), Prisma + Postgres, ClickHouse later, seeders**. Harvested joodcms patterns (Prisma 7 + PrismaPg, better-auth `emailOTP`, Docker/Postgres/ClickHouse, `Seeder<T>`).
- **2026-05-22** — Phases 1–2: app shell + all six surfaces built on mock data (4-layer: routes → `*Fn` → mock data-access). Added shadcn component set + `next-themes`; added success/warning/info `cva` variants to Button & Badge. All routes 200, typecheck clean.
- **2026-05-22** — Phase 4: Prisma 7 + Postgres (`prisma.config.ts` + PrismaPg adapter), schema migrated to `keenpix` DB, seed (3 projects/2 tokens/800 logs), `data-access` (projects/logs) wired to Prisma.
- **2026-05-22** — Phase 5: better-auth `emailOTP` + `tanstackStartCookies`, `/api/auth/$` handler, `getSessionFn`, `/app` route protection, OTP login UI; full sign-in verified.
- **2026-05-22** — Phase 6: `/api/keenpix` sharp transform (resize/re-encode/format-negotiate) + disk cache + SSRF/allowlist guards + request logging. Verified against a live Unsplash image.
- **2026-05-22** — Phase 7: analytics moved to real Prisma aggregations over `request_logs`. Phase 8: Docker (Dockerfile + compose + Postgres). Phase 3: marketing landing at `/`.
- **2026-05-22 (session 2)** — Sharp refactored into named functions (`src/lib/sharp/transform.ts`); transform API expanded to `w/h/q/fmt/fit/dpr/blur` (all clamped, verified with real images). Per-workspace **allowed hosts** add/remove (data-access + auth-gated fns + Settings UI), verified add→200 / remove→403 (allowlist checked before cache). Added `Vary: Accept` + redirect re-validation (SSRF hardening). **Docker verified end-to-end** (build/run/migrate/seed/transform/auth/SSRF in-container) — pinned `pnpm@10.30.3` + relaxed `minimumReleaseAge` + added `openssl`; compose Postgres on host 5433. Ran a **4-persona multi-agent test pass** (transform/security/auth/QA) — all green; cold-start 500 confirmed Vite-dev-only.

## Known hardening / follow-ups
- **DNS-rebinding**: SSRF guard validates DNS once; a rebinding allowlisted host could flip to an internal IP before `fetch`. Mitigated for redirects (re-validated per hop); full fix = pin the resolved IP into the dispatcher.
- **Subdomain-wildcard trust**: allowlist matches `host === allowed || host.endsWith('.'+allowed)` — every subdomain of an allowed host is trusted (intentional; tighten to exact-match if needed).
- Real OTP email provider (currently console); ClickHouse (Phase 9, deferred).
- `pnpm db:studio` is disabled (Prisma Studio's transitives were the radix source we purged). Use psql / TablePlus / drizzle-studio for ad-hoc DB inspection.

- **2026-05-22 (session 3)** — Cleanup. **Radix purged completely** via `pnpm.overrides` redirecting `@prisma/studio-core` to `npm:noop3@1000.0.0` (radix was only inside Prisma Studio's transitives; never in app code) — pnpm-lock count 25→0, node_modules/@radix-ui absent. **Swapped ESLint+Prettier → Biome+Ultracite** matching safhati/fluent-seo: 95 ESLint errors → **0** Biome errors after auto-format + 22 targeted fixes (file-level `noBitwiseOperators` suppression on the PRNG, hoisted regexes, extracted nested ternaries, fixed array-index keys, named imports, etc.). `pnpm lint` is now `biome check`, typecheck clean.

- **2026-05-30 (session 4 — production-readiness pass)** — Ran a 133-agent multi-lens audit (security/arch/perf/UX/observability/deploy + Kai/Lena/Mort personas, adversarial verification), then booted the app and fixed the findings end-to-end against a live dev server:
  - **Auth P0:** added `authMiddleware` to the four read server-fns (`getDashboardFn`/`listProjectsFn`/`listLogsFn`/`getAnalyticsFn`) — they shipped ungated, so direct RPC leaked projects/logs/analytics. Re-verified the authed dashboard still 200s.
  - **Secret P0:** `docker-compose.yml` now *requires* `BETTER_AUTH_SECRET` (`${VAR:?}`, verified it refuses to start when unset) instead of a hardcoded value; `lib/auth/server.ts` throws in production on a missing/known-weak secret and enables `useSecureCookies` on HTTPS.
  - **Transform DoS:** origin-size cap (50 MB → 413, streamed with a byte ceiling), concurrency gate (`KEENPIX_MAX_CONCURRENCY` + queue → 503), per-request undici `Agent` now closed in `finally` (socket-leak fix), `sharp.concurrency(1)` + `limitInputPixels` + `failOn:'truncated'`.
  - **Cache integrity:** atomic temp+rename writes, empty-file read guard, `KEENPIX_CACHE_MAX_BYTES` NaN guard (a malformed value was wiping the whole cache).
  - **SSRF:** `isPrivateIp` now unwraps IPv4-mapped IPv6 (`::ffff:127.0.0.1`) and covers CGNAT/multicast (unit-tested).
  - **IDOR:** `deleteTokenFn` scoped to org via `deleteMany`.
  - **Self-host:** SMTP OTP delivery (`lib/email.ts`, console fallback for dev/trials), `DATABASE_URL` boot guard, `/api/health` readiness endpoint + compose healthcheck + `restart: unless-stopped`, non-root (`USER node`) container.
  - **UX:** login double-submit guard; wired the dead sidebar "New project" control.
  - **Tests:** first unit tests (vitest) over the SSRF guard + cache key (9 passing); `pnpm lint` / `typecheck` / `test` all green. Real OTP login + transform (MISS→HIT) + SSRF (403) re-verified live.
  - **Deferred (documented):** project/org-scoped analytics & logs (the switcher shows global numbers for single-org v1), settings zero-state, separate one-shot migrate job for multi-replica.

- **2026-05-30 (session 4 cont. — Docker self-host validated + independent fix-verification)** — Brought the full stack up via `docker compose up --build` and drove every flow in the **production container**. Fixed three container-only blockers that only surfaced at runtime: (1) `pnpm exec prisma migrate` crashed as non-root (pnpm's implicit deps-check writes to `/app` → EACCES) → invoke the prisma CLI JS directly (`node node_modules/prisma/build/index.js`); (2) the `@prisma/studio-core` stub symlink dangled because the runner didn't copy `tools/` → added `COPY --from=build /app/tools`; (3) Nitro inlined `undici` and broke its `node:http2` connector ("http2.connect is not a function") so every HTTPS origin fetch failed → externalized undici via `nitro.config.ts` (`rollupConfig.external`). Plus a stale root-owned cache volume (dev-env only; fresh deploys seed `node` ownership from the image). **Verified in-container:** non-root (`node`), `/api/health` 200, migrate-on-boot, marketing/login/auth-gate (307), OTP sign-in → authed dashboard 200, `/api/keenpix` 200 (webp+avif, MISS→HIT) against live Unsplash, 401 without key, 403 on metadata-IP + off-allowlist.
  - Ran a second multi-agent **fix-verification workflow** (11 lenses, adversarial). It confirmed 7 fixes complete and caught **3 real holes, now fixed**: the `.env`'s own 44-char `dev-secret-change-me-…` passed the prod secret guard (exact-match list) → normalized substring check + 32-char floor; IPv6 `ff00::/8` multicast + `fec0::/10` site-local bypassed the SSRF guard → added (unit-tested); the redirect-drain `arrayBuffer()`'d the body uncapped and `agent.close()` pinned a concurrency slot ~10s on unread bodies → `body.cancel()` + `agent.destroy()`. Added transform error-cause logging (observability). `lint` / `typecheck` / `test` (9) green.

- **2026-05-30 (session 5 — keyless allowlist model + persona QA + OSS readiness)** — Per user direction, **removed API keys entirely**: `/api/keenpix` is gated solely by the per-project domain allowlist (deleted `lib/auth/api-key.ts`, `data-access/tokens.ts`, `functions/tokens.ts`, `features/projects/tokens.tsx` + the token UI; SSRF guard now **fails closed on an empty allowlist** → 403; endpoint requires `?project=` → 400 if missing). Ran a 7-agent **persona QA workflow** (first-time self-hoster, whitelist user, a11y, mobile, error-paths, OSS reviewer) — 50 findings, `readyForOSS: false` → now fixed: README rewritten keyless; the previously-hidden **Project ID is copyable in Settings**; dashboard gained an empty state + its mock activity no longer references removed features; Settings dropped the false "keenpix.config.json" claim, the dead Purge/Delete buttons, and disabled the non-persisting controls; landing CTAs → `/login` and stale "tokens" copy fixed; accessible names on login/allowlist/select inputs. Added a root **MIT LICENSE**. Browser-validated landing/login/a11y, then **rebuilt the production container and confirmed the full keyless flow**: transform 200 (no key), 403 off-allowlist, 400 no-project, OTP login, all authed pages render (dashboard/settings/analytics/logs 200), non-root. `lint` / `typecheck` / `test` (10) green. **Remaining polish (non-blocking):** `Token` model still in schema (unused — drop via migration); deeper a11y (aria-live on the log stream + allowlist, a skip-to-content link, sidebar `<nav>` landmark); General/Pipeline settings persistence; a placeholder SVG for `<img>` 403s.

- **2026-05-30 (session 6 — full browser QA + 2 orchestrated workflows + 22-fix pass)** — Drove the whole platform from the browser (not just curl): OTP sign-in, every `/app` page, create-project → add/remove allowed host (403↔200) → transform, responsive (375px) + the wide tables. Ran a **60-agent multi-persona audit workflow** (7 lenses: first-time self-hoster, multi-project ops, a11y, mobile, security/abuse, TanStack/React, correctness — each finding adversarially verified) → 24 deduped issues, then a **16-agent fix-verification workflow**. Landed fixes for ~22 of them, each re-verified live:
  - **Dev↔prod routing parity (was the #1 issue):** `/api/keenpix` 404'd in `pnpm dev` whenever the `url` ended in an asset extension OR the request carried `Sec-Fetch-Dest: image` (so **no `<img>` worked in dev**) — the dev static handler shadowed the route. Added a dev-only Vite middleware (`keenpixDevApiPassthrough`) that neutralizes both for `/api/keenpix`. **Production was verified unaffected** (ran the prod image on :3100 → handler runs; the earlier prod 500 was a Git-Bash MSYS path-mangling artifact of the test `docker run -e`, not a bug).
  - **Auth/abuse:** env sign-in allowlist (`KEENPIX_ALLOWED_EMAILS`/`_DOMAINS`, open+warn when unset — verified it blocks non-allowlisted) closing open-signup; `.env.example` secret now guard-rejected + `isWeakSecret` hardened; better-auth `rateLimit` enabled in dev with a 5/hr OTP-send rule + `ipAddressHeaders` (verified 429 on the 6th send).
  - **Fixture-data purge (the biggest trust problem):** deleted the fictional **Billing** page; dashboard cards now show **real per-project 24h requests + hit-rate** (dropped the `mulberry32` PRNG + hardcoded 92%); analytics drops Cost/L1-memory/variants/geo/top-domains fixtures and computes **real p50/p95/p99**; **Live logs** drops the synthetic `Math`-PRNG stream → polls real rows via `router.invalidate`, NDJSON export wired, row-key `id` (fixes a React dup-key crash); deleted dead `src/shared/mock.ts`.
  - **Project switcher made real (#4):** active project is now a `?project=` URL search param threaded route→`*Fn`→data-access (`where { projectId }`), so analytics/logs/dashboard actually scope (verified store=762 vs blog=187 live).
  - **Resilience:** `RouteError` boundary (`/app` `errorComponent` + router `defaultErrorComponent`) + `FormatDonut` empty-state guard (fresh-install analytics no longer blanks the page); `currentProject` typed `| undefined`.
  - **a11y/responsive:** AA `-text` token variants for status text (logs/analytics/badge/button), skip-to-content link + `<main>` landmarks (landing/login/app), `<Button render={Link}>` → `<Link className={buttonVariants()}>` (kills the Base UI `nativeButton` console error), Settings dead controls → read-only, dialog `DialogTrigger` ARIA, responsive `PageHeader`/filter-bar/settings rows, bigger touch targets + mobile Sheet close button, allowlist remove-toast + log `aria-live`.
  - **Hardening/cleanup:** sharp AVIF `effort:3` + bounded `MAX_DIMENSION` for no-w/h requests; SSRF guard now unwraps hex-compressed IPv4-mapped IPv6 (`::ffff:7f00:1`, unit-tested); README failure-mode table + docs `w` optional; removed the stale "Cache purged" activity + fabricated `v2.4.1`.
  - `lint` / `typecheck` / `test` (10) green throughout. **Deferred (documented):** `Token` model drop (needs a migration); transform worker-pool / in-flight coalescing (did the cheap AVIF-effort + size-clamp wins); Redis-backed rate-limit for multi-replica; cleaner 403 (vs 500) on a blocked sign-in.

- **2026-05-30 (session 7 — navigation/scope rebuild + Fumadocs)** — Per user direction, reworked the app shell and docs:
  - **Scope model (All ⇄ per-project):** `?project=` absent (or a stale id) now means **"All projects"** (org-wide) — the new default; a project id scopes. `stores/project-context.tsx` exposes `isAll` / `projectId|undefined` / `currentProject|undefined` / `setProject(id|null)` (replaces `setProjectId`). The data layer already threaded an optional `project`, so this was UI wiring, not a query rewrite.
  - **Sidebar → shadcn dashboard layout:** rebuilt `app-sidebar.tsx` using the installed shadcn (Base UI) `Sidebar`. New `components/app/project-switcher.tsx` (header scope switcher: "All projects" + each project + New project — the "website/team changer") and `components/app/nav-user.tsx` (footer account block). Footer holds **Documentation, Settings, and the user/account menu** (account, settings, docs, theme toggle, sign out). Workspace nav (Dashboard/Analytics/Live logs) + a Projects quick-list group.
  - **All-views (grouped by project):** Analytics gains a **By-project breakdown** table when `isAll` (`getProjectBreakdown(range)` in data-access; rows drill into a project); Logs gain a **Project column** when `isAll` (`LogRow.projectId`); Settings shows a **project-picker** empty-state when `isAll`. Topbar breadcrumb is scope-aware.
  - **New `/app/account` route** — profile (email/name), theme preference (light/dark/system), workspace info, sign out. User threaded from `/app` route context.
  - **Docs → Fumadocs at `/docs`** (replaces the old `/app/docs` tabs page, deleted): `fumadocs-core`/`-ui`/`-mdx` pinned **16.9.3 / 16.9.3 / 15.0.10** (Vite 7 + TanStack Router 1.x + React 19.2 + zod 4 all satisfied — **no core upgrade needed**). `mdx()` Vite plugin (first in the array), `source.config.ts`, `content/docs/*.mdx` (+ `meta.json`), `routes/docs/$.tsx` (DocsLayout + `createClientLoader`/`useFumadocsLoader`/`serializePageTree`), `routes/api/search.ts` (Orama). **Isolated to /docs:** its CSS (`src/styles/docs.css`) is linked only in the docs route `head`, and `RootProvider theme={{enabled:false}}` reuses the app's next-themes — the rest of the app is visually untouched (verified). `collections/*` → `.source/*` (gitignored; generated by `fumadocs-mdx`; `typecheck` now runs `fumadocs-mdx &&` first). **Note:** `fumadocs-ui` re-introduces Radix (scoped to /docs) — a conscious reversal of the earlier "Radix purged" state, accepted by the user.
  - Browser-verified (DOM/snapshot — screenshots don't capture in this headless preview): All-mode + per-project scoping (store=blog switch), analytics breakdown, logs Project column, account page, settings picker, `/docs` index + sub-pages + MDX tables + TOC + `/api/search` (200, ranked). `lint` / `typecheck` / `test` (10) / `pnpm build` all green.

- **2026-05-30 (session 8 — dashboard-01 dashboard, ported to Base UI)** — User asked for `npx shadcn@latest add dashboard-01`. **The CLI is incompatible here** — `dashboard-01` is a default-registry **Radix** block; the add prompted to overwrite `button.tsx`/`card.tsx`/etc., which would clobber the **custom success/warning/info `cva` variants** on Button/Badge and break the Base UI app. (A Base UI flavor exists at `shadcn-ui/ui` → `apps/v4/registry/bases/base/blocks/dashboard-01` — used as a **port reference**, not via the CLI.) The CLI did add `@dnd-kit/*` + `@tanstack/react-table` + `vaul`; **kept `@tanstack/react-table`, removed dnd-kit + vaul.** Then ported dashboard-01's signature layout to Base UI + real data:
    - `components/app/section-cards.tsx` — 4 KPI cards (Total requests / Bandwidth saved / Cache hit rate / p95) with **real this-vs-previous-window trend badges** (`getDashboardKpis(range, project)` in data-access; shows "New" honestly when there's no prior-window baseline — no fabricated deltas).
    - `components/app/chart-area-interactive.tsx` — gradient area chart (cache hits vs optimized) with a range ToggleGroup.
    - `components/app/projects-data-table.tsx` — `@tanstack/react-table` (sortable columns) + env `Tabs` filter; rows select a project.
    - Dashboard page (`routes/app/dashboard/index.tsx`) recomposed to `SectionCards + ChartAreaInteractive + ProjectsDataTable`, **scope-aware** (All = global KPIs + all projects; a project = its KPIs/chart) with `range` + `project` search params; `getDashboardFn` now returns `{ range, projects, stats, kpis, series }` (dropped the unused recent-activity).
    - Switched the app shell to dashboard-01's **`variant="inset"`** sidebar + `SidebarProvider` CSS vars (`--sidebar-width` 18rem, `--header-height`). Because the dashboard's `validateSearch` makes `range` required, `/app/index` redirect + login navigate now pass `search:{ range:'30d' }`.
    - Browser-verified: KPIs/chart/table render; scope `?project=blog&range=7d` produces distinct KPIs (278 vs 1.1K All); range toggle + env tabs + inset shell work. `lint` (85) / `typecheck` / `test` (10) / `pnpm build` (16.8s) all green.

- **2026-05-30 (session 8 cont. — fixed broken sidebar menus/dialogs)** — User reported "clicking the menu or dropdown does nothing." Manual browser test (preview MCP) found **two Base UI mistakes** that broke *every* DropdownMenu/Dialog trigger I'd written:
    1. **Children inside the `render` element.** I wrote `<DropdownMenuTrigger render={<SidebarMenuButton size="lg">…children…</SidebarMenuButton>} />`. Base UI's canonical pattern is the inverse — the `render` element is a **self-closing shell** and the content is the **trigger's** children: `<DropdownMenuTrigger render={<SidebarMenuButton size="lg" />}>…children…</DropdownMenuTrigger>`. With children-in-render the trigger never wires its open handler (`aria-expanded` stayed `false`, menu never opened). Fixed in `project-switcher.tsx`, `nav-user.tsx`, and `new-project-dialog.tsx` (`DialogTrigger`).
    2. **`DropdownMenuLabel` (= Base UI `Menu.GroupLabel`) used without a `DropdownMenuGroup`.** Once the menu opened, rendering the bare label threw `"Base UI: MenuGroupContext is missing"`, which the `/app` `errorComponent` caught and replaced the **entire shell** with "Something went wrong" (looked like the sidebar vanished). Wrapped every label+items section in `<DropdownMenuGroup>` (project-switcher + nav-user).
    - Verified live: switcher opens (All projects + 4 projects + New project) and selecting one sets `?project=`; nav-user opens (Account/Settings/Documentation/theme/Sign out); New-project dialog opens with its form — all with **no error boundary**. `lint` (85) / `typecheck` / `test` (10) / `pnpm build` (15.5s) green.
    - **Preview-env caveat (for future browser testing):** this headless preview is flaky — `window.innerWidth` reports 0 until an explicit `preview_resize` + reload (→ `useIsMobile` flips the sidebar to a closed mobile Sheet, so the desktop trigger "disappears"); `preview_screenshot` times out; and the DOM churns between separate `eval` calls (do click+inspect in ONE self-contained async `eval`). A route `errorComponent` makes any child render-throw blank the whole shell.

- **2026-05-30 (session 8 cont. — settings, filters, account, sidebar cleanup)** — Per user feedback:
  - **Sidebar:** removed the redundant "Projects" quick-list; moved **Settings into the Workspace nav** (after Live logs); removed Settings from the footer and from the nav-user dropdown (footer now = Documentation + account block).
  - **Real per-project pipeline settings:** added `autoFormat`/`stripMetadata`/`defaultQuality` to the `Project` model (migration `add_project_pipeline_settings`) + `updateProjectSettings` data-access + `updateProjectSettingsFn`. The Settings → Pipeline card now has working **Switches** (auto-format, strip-metadata) + a **Default quality** input, persisted on change (verified the toggle survives reload). **Wired into the transform:** `defaultQuality` is used when `?q=` is omitted; `autoFormat` off ⇒ no Accept negotiation (serves JPEG for `fmt=auto`); `stripMetadata` false ⇒ sharp `.keepMetadata()`.
  - **Filters (reui.io-style, built on Base UI DropdownMenu):** new `components/app/faceted-filter.tsx` (count badge + clear). **Logs** gets Format/Status/Cache/Region facets (client-side; Region hidden when no region data) + an "N of M" count. **Analytics** gets a server-side **Format** filter threaded via a `?format=` search param → `getAnalyticsFn` → `scope()` (re-aggregates). Verified: logs webp filter → 16/36 rows all webp; analytics avif → URL `?format=["avif"]`, refetch, no error.
  - **Card titles enhanced** (analytics: "Requests/Bandwidth/Cache hit rate over time", "Cache performance", "Response latency"; clearer section-card captions).
  - **Account page expanded** (`/app/account`): editable display name (better-auth `authClient.updateUser` + `router.invalidate`), Appearance (theme), Security (Email OTP / passwordless badges), Workspace (projects + self-hosted), sign out.
  - **Gotcha:** Windows file lock — `prisma migrate dev`'s auto-`generate` was blocked by the running dev server, leaving a stale client; **stop the preview server, run `prisma generate`, restart**. `lint` (87) / `typecheck` / `test` (10) / `pnpm build` (12.8s) all green.

- **2026-05-30 (session 8 cont. — docs restructure + framework matrix)** — Replaced the flat Fumadocs content with a **nested tree** (folder `meta.json` + Lucide `icon` per section): **Getting started** (quickstart, projects/keyless), **How it works** (how-it-works lifecycle, caching, security/SSRF, analytics — the "explain how this works" ask), **Frameworks** (a universal `keenpix()` URL-builder + `srcset` helper, then 9 guides: Next.js loader, Nuxt `@nuxt/image` provider, TanStack Start, React/Vite, Vue, SvelteKit, Astro, Remix, plain HTML/any backend), **API reference** (endpoint, full parameter table, responses/errors), **Self-hosting** (Docker deploy, env-var configuration). Used Fumadocs `<Callout>`/`<Cards>`/`<Card>` (confirmed present in `defaultMdxComponents`). Browser-verified the nested sidebar, a deep page (`/docs/frameworks/nuxt` → code + auto-TOC + all 9 framework links), search, no errors; `typecheck` / `pnpm build` (12.4s) green.

- **2026-05-30 (session 8 cont. — self-host mode, ReUI filters, polish)** — Per user feedback:
  - **Quality input clamp:** the Settings → Pipeline Default-quality input now clamps the **max on change** (typing 5000 becomes 100) and the **min on blur** (5 becomes 30), so it can't be set out of range. (Verified live.)
  - **Self-host mode:** new `KEENPIX_SELF_HOST` env (+ `getPublicConfigFn` in `functions/config.ts`). When `true`, the marketing landing (`/`) is replaced by a minimal "Self-hosted Keenpix" splash with **Go to dashboard** + **Documentation** buttons. Documented in `.env.example` + `docs/self-hosting/configuration`. Verified: flag on -> splash; flag off -> marketing.
  - **Removed the dashboard card gradients** (the `bg-gradient-to-t from-primary/5 to-card` on the KPI section cards) — plain cards now.
  - **Rebuilt the filters to match reui.io** (the earlier per-field dropdowns weren't the requested "add filter + pills" UX): new `components/app/data-filters.tsx` — a **"Filter / Add filter"** button that lists fields, plus a removable **pill per active field** (label, value summary, remove) and a Clear. Replaced `FacetedFilter` (deleted). **Logs**: format/status/cache/region (client-side). **Analytics**: format + status (server-side; `status` added to `scope()` + threaded via `?status=`). Verified: logs Format=webp -> 16/36; analytics Status -> `?status=["404"]`.
  - `lint` (93) / `typecheck` / `pnpm build` (16.4s) green.

- **2026-05-30 (session 9 — full validation/QA pass + cleanup + error-handling hardening)** — Tested every flow/page end-to-end (hands-on HTTP + DB, then a 29-agent adversarially-verified Workflow) and fixed what it surfaced.
  - **Cleanup:** deleted the superseded prototype (`extracted/` + `open-images-optimization.zip`) and the stale `.cta.json` scaffold record; removed two orphan modules (`data-access/events.ts` + its `ActivityEvent` type; `components/app/sparkline.tsx`); pruned a leftover throwaway DB project and reseeded canonical state (3 projects / 800 logs).
  - **Validation results:** SSRF 78/78, auth 19/19, docs+search 234/234, analytics 29/29, deploy 27/27, transform correctness 72/72; OTP login → all authed pages 200; create→allowlist(403↔200)→settings-persist 9/9; dark/light/mobile (no overflow) all green.
  - **Error-status mapping (the dominant fix — origin failures were leaking as a generic 500)** in `actions/transform/handle-transform.ts`: origin **timeout → 504**, **connection/network error → 502**, **non-image/undecodable body → 502** (was 500 leaking sharp/undici internals); any non-`TransformError` 5xx now returns a generic message (no internal leak); **cache-write is best-effort** (a failed write no longer 500s a good transform); new **`KEENPIX_ORIGIN_TIMEOUT_MS`** (default 10000). README failure-mode table + `.env.example` updated (504, `KEENPIX_MAX_DIMENSION`, the new timeout; corrected the `KEENPIX_MAX_CONCURRENCY` "default 4" comment → real default = CPU count).
  - **Other fixes:** `removeAllowedOrigin` is now an atomic `$transaction` (was a lossy read-modify-write); analytics **status filter** with all-invalid values is a no-op (was `in: []` → blank zero-row page); widened `LogStatus` → `number` and added `png` to `LogRow.format` (the `logs.ts` casts were lying), plus the real status codes (400/403/413/500/502/503/504) in the analytics Status filter; **removed the half-wired Region column** from Live logs (the live transform never writes region/country — geo remains a documented roadmap item); fixed the dangling "and in activity" copy on `/app/account`; **`docker-compose.yml`** Postgres password is now overridable (`${POSTGRES_PASSWORD:-passer}`) instead of a hardcoded literal.
  - `lint` (90) / `typecheck` / `test` (10) / `pnpm build` all green on the final cleaned tree. (The workflow's two "cookie" blocker/major findings were a test-harness artifact — the shared session cookie was signed out mid-run by the auth-abuse agent — not a product bug.)

- **2026-05-31 (session 10 — CDN-first endpoint + self-host CDN docs)** — Moved the public transform route from `/api/keenpix?url=...` to **`/img/<source-url>?project=...`** while preserving the same sharp/cache/allowlist pipeline. This fixes the Cloudflare caveat by keeping the source file extension in the request path and makes the endpoint look like image delivery instead of an API route. Added a self-host **CDN setup** doc covering Cloudflare Cache Rules (`/img/*`, full query string, no dashboard/auth caching), non-Cloudflare CDN guidance, and explicit `fmt` recommendations when a CDN cannot vary cache by `Accept`. Updated README, API reference, framework examples, docs search tree, dashboard/settings copy, and dev-server passthrough. Validated with `pnpm test`, `pnpm run typecheck`, `pnpm lint`, `pnpm build`, `pnpm run knip`, `pnpm run doctor`, browser checks for the self-host splash and CDN docs, and GitHub CI/Docker image checks. Merged as PR #8.

- **2026-05-31 (session 11 — form/schema hardening and implementation status refresh)** — Consolidated form validation around **TanStack Form + zod schemas** for staff invitations, SMTP settings, project creation, allowlist hosts, pipeline settings, analytics/log filters, docs/admin/auth/common env inputs, and invitation acceptance. Added shared form-error extraction, improved common error-message parsing, and wired TanStack devtools panels for Query/Router/Form/Pacer. Tightened admin/staff/project forms with field-level validation, clearer disabled/pending states, safer env parsing, and cleaner route/search validation. Updated this implementation status so the living doc matches the `/img/*` endpoint and current product shape.

- **2026-06-13 (release catch-up — v0.1.4 through v0.1.7 + unreleased edge/origin work)** — Current product now includes internal API keys + `/api/sdk`, app Settings sections for project and global configuration, source-domain analytics, SSE live logs, operations cache/queue health, IPX-style Sharp modifiers, SVG optimization, and Cloudflare GraphQL edge-cache analytics. Latest commits redesign Overview and Analytics around a Cloudflare edge → Keenpix disk cache → live origin funnel, with separate edge/origin source-split cards and chart lenses so zone-level edge data is not mixed with project-scoped origin trends.
