# Keenpix Cloud production migration record — July 17, 2026

Status: verified bounded migration; legacy production remains intact and live

This record supersedes the preparation-only status in the migration-readiness
runbook for the explicitly authorized July 17 migration. It does not authorize
retiring, restarting, reconfiguring, or deleting the legacy deployment.

## Manifest

- Run ID: `keenpix-joodlab-20260717t1000z`
- Inclusive source scope: organization `org_default` and its selected project
- Target organization: the existing Joodlab cloud organization
- Upper bound: records strictly before `2026-07-17T10:00:00Z`
- Source release: `e8879d1e2e4839e86d6369b8266b6a60b591001f`
- Target release at migration: `fab86888498db1875906cf047ff011435150d53e`
- Batch size: 2,000
- Mode: repeatable-read source transaction and idempotent target upserts

Credentials, database addresses, API-key values, and owner identifiers are
intentionally excluded from this repository record.

## Verified result

The production run copied and content-reconciled:

| Dataset | Bounded source rows | Verification |
| --- | ---: | --- |
| Request logs | 2,776,462 | no missing, extra, or mismatched migrated rows |
| Hourly analytics rollups | 2,134,980 | no missing, extra, or mismatched migrated rows |
| API-key activity | 1,901 | no missing, extra, or mismatched migrated rows |

The verifier also confirmed the selected API key and its project scope are
usable in the target organization. Three request-log rows and two hourly-rollup
rows created natively in cloud were detected by ID and preserved outside the
legacy fingerprint. No cloud-native row was deleted or overwritten.

The manifest-scoped checkpoints were removed only after all fingerprints and
API-key checks passed. The checkpoint table remains available for a later,
separately identified bounded delta.

## Safety and remaining delta

The source transaction was read-only. The legacy application, Postgres
container, routes, and volumes were not stopped or changed and remain available
for rollback. Because legacy is still accepting traffic, records at or after the
10:00 UTC bound remain there by design; a later final delta requires a new run
ID and exact upper bound.

The legacy deployment has no ClickHouse service, so there was no legacy
ClickHouse dataset to copy. Its historical analytics are represented by the
reconciled Postgres request logs and hourly rollups. The cloud ClickHouse data
is cloud-native and was left unchanged.
