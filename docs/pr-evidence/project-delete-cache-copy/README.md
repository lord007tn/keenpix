# Project deletion cache copy

These screenshots render the exact source-extracted project deletion dialog with the real shared components and stylesheet, using a synthetic project name. Captured in Chrome at 320x900 and 1440x900. Both were visually inspected; mobile dialog bounds were x16..304, y270..630 with no document overflow. This is local visual evidence, not a production deletion or cache-purge test. No interaction or motion changed, so a recording adds no meaningful evidence.
