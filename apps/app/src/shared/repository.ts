// Public repository links. Client-safe on purpose: no env or server imports, so
// both client components and server-side metadata can share one source of truth.
export const REPOSITORY_URL = 'https://github.com/lord007tn/keenpix'
export const RELEASES_URL = `${REPOSITORY_URL}/releases`
