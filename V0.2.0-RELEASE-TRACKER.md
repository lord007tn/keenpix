# Keenpix Cloud v0.2.0 release tracker

Updated: 2026-07-13
Scope: `cloud` branch and cloud-owned services only. Legacy production and its data are forbidden for access, testing, mutation, deployment, restart, or migration.

## Acceptance gates

- [x] Validated P0/P1 SEO, content, comparison, UX, mobile, and performance work is implemented and independently reviewed.
- [x] Ten-post blog, trust surfaces, visible authors/methodology, and article-specific social imagery are production-ready.
- [x] Google OAuth, dedicated Keenpix GA4/GTM, Search Console, consent, funnel events, and Web Vitals collection are configured and verified as far as data/provider state permits.
- [x] Cloudflare canonical routing, TLS, cache scope, security headers, DMARC, and Coolify cloud-only deployment are verified.
- [x] Polar production products, prices, lifecycle webhook, entitlement mapping, usage idempotency, portal recovery, and failure paths are verified; sandbox apex callback configuration and invalid-signature rejection pass.
- [x] Postmark `raed`, `keenpix.com`, sender/stream, cloud environment, and one low-risk delivery are verified.
- [x] Organization → project → API-key ownership is relationally enforced and tested; production plan gating is verified live.
- [x] Full local/CI, live health, SEO drift, responsive browser, auth, billing, email, consent, and deployment checks pass.
- [x] v0.2.0 version/docs are present; logical commits are pushed to `cloud`; PR #57 is open and unmerged.
- [x] Migration-readiness package exists; execution and legacy access remain deferred.

## Live issue register

| ID | Priority | Workstream | Status | Evidence / resolution |
| --- | --- | --- | --- | --- |
| SEO-01 | P0 | Canonical and indexability | Resolved | Cloudflare one-hop 308 matrix to HTTPS apex passes; canonical, robots, 60-URL sitemap, schema, Open Graph, and drift checks pass. |
| UX-01 | P0 | Narrow mobile | Resolved | Homepage, blog, pricing, comparison, and JoodCMS article verified at 320–1440 px; final long inline-code wrapping fix removes the article-only overflow. |
| PERF-01 | P1 | Performance/RUM | Implemented | Font preload, reduced public payload, consent-aware GA, and sampled LCP/INP/CLS collection ship. Field improvement is not claimed before traffic accumulates. |
| CONTENT-01 | P1 | Blog/trust/GEO | Resolved | Ten sourced posts, About/security/status/support/author/methodology pages, visible bylines, primary-source dates, and disclosed JoodCMS first-party evidence are live. |
| COMP-01 | P1 | Comparisons | Resolved | Six comparison pages distinguish Keenpix scope from competitor facts and use primary sources, verification dates, license timing, and explicit methodology. |
| DATA-01 | P0 | OAuth/analytics/search | Resolved with provider limitation | OAuth completes to the dashboard. GA4 and GTM are dedicated Keenpix accounts. Direct GA4 consent gating passes. GTM native tag remains scanner-paused; GSC recrawl is pending. |
| INFRA-01 | P0 | Cloudflare/Coolify | Resolved | Strict TLS, cache bypass, headers including 404, DMARC, apex routing, healthy cloud deploy, and removal of deprecated sandbox-subdomain DNS are verified. |
| BILL-01 | P0 | Polar | Resolved with provider limitation | Three monthly products, meter/credits, production signature/config, webhook/entitlement tests, checkout handoff, and both apex endpoints are verified. Signed sandbox delivery remains unavailable in Polar’s endpoint-details UI. |
| EMAIL-01 | P0 | Postmark | Resolved | `raed` server and `keenpix.com` sender/domain/stream configured; invitation to `fariq@keenpix.com` recorded as Delivered. |
| AUTH-01 | P0 | Tenant isolation | Resolved | Project ownership, API-key scope, stale-org recovery, membership rechecks on streams, and cross-org denial are enforced and tested. |
| RELEASE-01 | P0 | Release | Complete | Runtime `43a6004` deployed in `eonl8l4mkek0r37ne4vnrxin`; CI passes; PR #57 remains open and unmerged. |
| MIG-01 | Deferred | Migration | Deferred | Readiness package is NO-GO until later explicit authorization. No legacy access or cutover occurred. |

## Evidence log

- Workspace confirmed as `D:\Work\keenpix` on branch `cloud`; pre-existing untracked planning/report artifacts preserved.
- `AGENTS.md`, migration plan, SEO roadmap/index, `brand.md`, and linked validated SEO/page/technical/content/schema/sitemap/image/performance/visual/GEO/competitor/cluster/SXO/FLOW/drift/Google/image-generation reports were read and reconciled. Scanner false positives and unsupported claims were rejected.
- Cloudflare canonical apex matrix, Full (Strict), TLS 1.2 minimum, `/app` and `/api` cache bypass, response headers on a fresh 404, and DMARC reporting to `fariq@keenpix.com` passed. Deprecated `polar-sandbox.keenpix.com` DNS was deleted; no webhook subdomain is used.
- Dedicated Google resources: GA account `Keenpix` (`400817194`), property `Keenpix Cloud` (`545156185`), stream `15243494513`, `G-C04VQED7GV`; GTM account `Keenpix` (`6365464774`), container `GTM-TFJ9TQDN`; GSC domain property and GA4↔GSC link configured.
- Production OAuth client has only apex origin and callback. End-to-end Google sign-in reached `/app/dashboard?range=30d`.
- Consent denial resulted in zero GA scripts; consent acceptance loaded exactly one direct GA4 script for `G-C04VQED7GV`.
- Polar production and sandbox endpoints are apex paths. Both reject unsigned JSON with 400. Production has exactly Basic $9, Pro $19, and Business $29 monthly products and correct lifecycle configuration. Checkout handoff showed the expected Basic monthly 14-day trial; QA stopped before charge.
- Postmark invitation to `fariq@keenpix.com` was recorded as Delivered through the `raed` server.
- Cloud health returned 200 with all service checks healthy. Live sitemap contains 60 URLs and includes `/blog/joodcms-keenpix-integration`.
- Fresh Windows SEO drift baseline and immediate comparison passed with no critical or warning differences; only the ignored raw HTML hash changed.
- Live public browser matrix covered homepage, blog, JoodCMS article, pricing, and Cloudinary comparison at 320, 360, 375, 390, 412, 768, and 1440 px. Controls measured 48×48 px; article long-code overflow was found and fixed before final deployment.
- Organization/project/API-key integration tests cover ownership and cross-org denial. The live account has no subscription and correctly blocks project creation; no paid trial was started solely for QA.
- Logical release commits `c7dae54`, `cb27a04`, and `43a6004` are pushed. Runtime `43a6004` is deployed; final release-evidence/mobile-fix commit follows after full gates.

## External limitations and post-release observation

- Google must clear the scanner-paused native GTM tag; direct consent-gated GA4 is the current primary path.
- Real GA4 funnel rates, GSC indexing/60-URL recrawl, and field Core Web Vitals require accumulated traffic.
- Polar’s endpoint-details UI did not expose a usable signed sandbox delivery control. Configuration, signature rejection, production behavior, and application tests are complete; do not invent a provider-signed event.
- Live organization/project/API-key mutation requires an active plan. Starting a real production trial only for QA was intentionally avoided.
- Customer proof, ratings, certifications, benchmarks, and universal savings are not claimed.
- PR #57 must not be merged without explicit owner instruction.
- Migration/cutover and all legacy access require a later explicit owner instruction.
